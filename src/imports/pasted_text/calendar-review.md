I'll take a look at the calendar project you built.

עברתי על כל הקוד — `App.tsx`, `hebrew-calendar.ts`, `index.css`. הבסיס באמת יפה: הטיפוגרפיה (Frank Ruhl Libre + נייר עם גריין), השימוש ב-`@hebcal/core` במקום חישובים ביד, ו-CSS variables מסודרים. להלן מה שמצאתי, מהחשוב לפחות.

## באגים אמיתיים (הייתי מתקן קודם)

**1. שדה ה-`note` נשמר אבל לא מוצג באף מקום.** יש textarea "מה קרה ביום הזה?" ב-[App.tsx:567](C:/Users/משתמש/Downloads/לוח שנה/src/App.tsx:567), אבל `entry.note` לא מרונדר בסיידבר ולא בתא היום — רק ב-`title` וב-`image`. כלומר המשתמש כותב זיכרון ואחרי שמירה הוא נעלם עד שפותחים שוב את העורך. זה הפיצ'ר הרגשי של הלוח והוא כרגע write-only. הוסף אותו לכרטיס "אירוע אישי" בסיידבר.

**2. חצי הניווט בין החודשים הפוכים.** ב-[App.tsx:216](C:/Users/משתמש/Downloads/לוח שנה/src/App.tsx:216) הכפתור הראשון ב-DOM הוא `onNext` עם הגליף `›`. ב-`justify-between` תחת `dir="rtl"` הוא נופל ל**ימין** — כלומר "החודש הבא" יושב בימין ומצביע ימינה, שזה כיוון "אחורה" בעברית. החלף בין השניים (או השאר את המקום והפוך את הגליפים).

**3. localStorage יתפוצץ על תמונה אחת מהטלפון.** התמונות נשמרות כ-data URL בתוך אותו מפתח JSON ([App.tsx:499](C:/Users/משתמש/Downloads/לוח שנה/src/App.tsx:499)), ו-base64 מנפח ב-33%. תמונה אחת מאייפון (~4MB) חורגת ממגבלת ה-~5MB, ה-`useEffect` בשורה 64 זורק `QuotaExceededError` בלי `try/catch` — והמשתמש מאבד את האירוע בלי שום הודעה. תיקון מינימלי: להקטין את התמונה ל-~1200px דרך canvas לפני השמירה, ולעטוף את הכתיבה ב-`try/catch` עם הודעה בעברית. תיקון נכון: IndexedDB לתמונות (בלי מגבלת 5MB), localStorage רק למטא-דאטה.

**4. אין בכלל "היום" בלוח.** `HEBREW_YEAR = 5787` קבוע, והתאריך היום הוא אלול תשפ״ו — כך ש-`todayLocation` מחזיר `null`, אין תג "היום" באף תא, והלוח נפתח על תשרי עם א׳ מסומן. בנוסף `todayIso` מחושב פעם אחת ב-`buildYear` ולא מתעדכן בחצות. הצע: לחשב את השנה העברית הנוכחית דינמית (`new HDate().getFullYear()`) עם מחליף שנים ‹ ›, ולא לקודד 5787.

**5. `isYomTov` מחושב ולא בשימוש.** [hebrew-calendar.ts:93](C:/Users/משתמש/Downloads/לוח שנה/src/hebrew-calendar.ts:93) — ימי יו״ט נראים כמו יום חול רגיל. הם צריכים רקע משלהם, כמו לשבת.

**6. `src/imports/image.png` — 2.3MB שלא מיוזכר בשום import.** גם `image-1.png`. שאריות מ-Figma Make, אפשר למחוק.

## תוכן שחסר ללוח עברי

התאים הם `aspect-square` עם המון אוויר, וב-`@hebcal/core` כבר יש את כל זה בלי תלות נוספת:

- **ספירת העומר** — 49 ימים בין פסח לשבועות בלי שום ציון. `new Omer(hd)` נותן גם "ה׳ בעומר" וגם את הספירה המלאה. זו ההשמטה הבולטת ביותר.
- **דף יומי / משנה יומית / רמב״ם** — `DafYomi`, `MishnaYomi` מ-`@hebcal/learning`. בשורה קטנה בתחתית התא, או בסיידבר.
- **מולד ושבת מברכים** — `HDate.molad()`. זה מה שאנשים מחפשים בלוח קיר.
- **רק חג אחד מוצג ליום.** [hebrew-calendar.ts:91](C:/Users/משתמש/Downloads/לוח שנה/src/hebrew-calendar.ts:91) בוחר `primary` ומשמיט את השאר — ז׳ חנוכה שהוא גם ר״ח טבת יראה רק אחד מהם. שמור את המערך המלא והצג את השני בסיידבר.
- **הדלקת נרות רק בשישי.** [hebrew-calendar.ts:185](C:/Users/משתמש/Downloads/לוח שנה/src/hebrew-calendar.ts:185) בודק `dow === 5` — אין נרות בערב יו״ט, ואין הבדלה במוצאי יו״ט. וגם: `date.getDay()` עובד לפי אזור הזמן של הדפדפן בעוד הפורמט הוא `Asia/Jerusalem` — למשתמש בחו״ל זה יזוז ביום.
- **זמני תעניות** — תחילת/סוף צום לי״ז בתמוז, ט׳ באב, צום גדליה, עשרה בטבת.
- **תשפ״ז קבוע גם במפתח האחסון** (`hebcal-5787-events`), ואירועים מקושרים לתאריך לועזי — כך ש**יום הולדת עברי או יום זיכרון לא חוזרים בשנה הבאה**. שמור אירועים חוזרים לפי `(יום, חודש)` עברי עם דגל `recurring`, זה הפיצ'ר שיהפוך את זה מלוח חד-שנתי לכלי קבוע.

## נגישות ומקלדת

- **הגריד הוא ערימת `<button>`** בלי `role="grid"`, בלי `aria-current="date"` על היום, ובלי ניווט בחצים בין ימים (roving tabindex). Tab עובר כרגע 380 תאים אחד-אחד.
- **לתאים אין שם נגיש קריא** — קורא מסך שומע "כ״ה 14" בלי הקשר. `aria-label` עם התאריך המלא + חג + אירוע יפתור.
- **המודל אינו dialog** — אין `role="dialog"`, `aria-modal`, מיקוד לא נכנס פנימה ולא חוזר לכפתור בסגירה, ואין focus trap. Escape כן עובד, טוב.
- `text-[8px]` לשמות חגים ([App.tsx:324](C:/Users/משתמש/Downloads/לוח שנה/src/App.tsx:324)) קטן מדי מכדי להיקרא. 10-11px מינימום.
- `* { scrollbar-width: none }` הגלובלי ב-[index.css:39](C:/Users/משתמש/Downloads/לוח שנה/src/index.css:39) מסתיר את הסקרולבר גם מרצועת 13 החודשים — אין שום רמז שהיא נגללת, וגם אין `scrollIntoView` על הטאב הפעיל.

## שיפורי UX משמעותיים

- **גיבוי וייצוא.** כל התמונות והזיכרונות יושבים ב-localStorage של דפדפן אחד — ניקוי היסטוריה מוחק הכול. כפתור "ייצוא JSON" + "ייבוא" זה חצי שעה עבודה מול אובדן בלתי הפיך.
- **הדפסה.** לוח שנה זה הדבר הקלאסי שרוצים להדפיס או לשמור כ-PDF, ואין `@media print`. גם רקעי ה-Unsplash הם הוטלינק חיצוני — הם לא יודפסו ולא יעבדו אופליין.
- **חיפוש / תצוגת רשימה.** אין דרך למצוא אירוע בין 13 חודשים חוץ מדפדוף. חיפוש חופשי + "האירועים הקרובים" בסיידבר.
- **מצב כהה** — ה-CSS variables כבר מושלמים לזה, בלוק `@media (prefers-color-scheme: dark)` אחד עם 9 משתנים וזה עובד.
- **אי אפשר להסיר תמונה** אחרי שהוספת, רק להחליף.
- **קליק על הרקע זורק עריכות בשקט** ([App.tsx:509](C:/Users/משתמש/Downloads/לוח שנה/src/App.tsx:509)) — כדאי אישור אם יש שינויים לא שמורים.
- **החלפת חודש קופצת תמיד לא׳** (`setSelectedIso(null)` בשורה 88) — עדיף לשמור את אותו יום בחודש.
- **רק שתי עיירות**, ו-Israel מקודד קשיח (`new Sedra(year, true)`). תיבת בחירה מרשימת `@hebcal/core` + מתג ישראל/חו״ל.

## קוד

- `e.getFlags() & 0x1` בשני מקומות — יבא `flags` מ-`@hebcal/core` והשתמש ב-`flags.CHAG`.
- `.replace(/\s*\d{3,4}\s*$/, '')` בשורה 92 שובר את שם החג כדי להוריד את מספר השנה — שביר. עדיף אפשרויות הרינדור של hebcal.
- `App.tsx` הוא 607 שורות עם 5 קומפוננטות. פיצול ל-`components/` יעזור כשתוסיף עומר/דף יומי/חיפוש.
- אין tests. ולוגיקת לוח עברי היא בדיוק סוג הקוד שכדאי לכסות: קפיצות שנה, אדר א׳/ב׳, י״ג חודשים, חגים מתנגשים.

**אם הייתי בוחר שלושה:** התמונות שמפילות את localStorage (#3, אובדן דאטה), ה-`note` שנעלם (#1, הפיצ'ר הכי אישי בלוח), וספירת העומר + אירועים חוזרים לפי תאריך עברי — כי הם מה שהופך את זה מפרויקט Figma יפה ללוח שמשתמשים בו כל שנה.

